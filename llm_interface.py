def generate_text(prompt):
    """
    Generate text using an LLM based on the given prompt.
    This is a placeholder that returns "hello world" for now.
    
    Args:
        prompt (str): The input prompt for the LLM
        
    Returns:
        str: Generated text from the LLM
    """
    # Currently hardcoded, but later will connect to actual LLM
    return "hello world" 